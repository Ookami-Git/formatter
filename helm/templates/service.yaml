apiVersion: v1
kind: Service
metadata:
  name: {{ include "formatter.fullname" . }}
  labels:
    {{- include "formatter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "formatter.selectorLabels" . | nindent 4 }}
