{{- range .Values.app.configs }}
{{- if and . .id (eq .source "git") .git .git.token (not .git.existingTokenSecret) }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "dynamic-form.fullname" $ }}-git-token-{{ .id }}
  labels:
    {{- include "dynamic-form.labels" $ | nindent 4 }}
type: Opaque
data:
  GIT_TOKEN: {{ .git.token | b64enc | quote }}
{{- end }}
{{- end }}
